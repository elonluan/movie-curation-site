{
  "name": "curated-movie-archive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:data": "node scripts/sync-notion.mjs",
    "predev": "npm run sync:data",
    "prebuild": "npm run sync:data",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  }
}
